# === Base de données ===
psycopg2-binary==2.9.9        # driver PostgreSQL pour Python
#sqlalchemy==1.4.52            # ORM et connexion base de données
sqlalchemy==2.0.30
geoalchemy2==0.15.1           # extension PostGIS pour SQLAlchemy

# === Ingestion & manipulation données ===
requests==2.31.0              # téléchargement fichiers / appels API
pandas==2.2.0                 # manipulation de données tabulaires
geopandas==0.14.3             # manipulation de données géographiques
fiona==1.9.4                  # format de lecture/écriture géospatial
pyproj==3.6.1                 # projections cartographiques
shapely==2.0.3                # manipulation de géométries (dépendance geopandas)

# === Transformation ===
dbt-core==1.8.1               # force version stable (pas alpha)
dbt-postgres==1.8.1           # transformations SQL versionnées
protobuf>=4.0,<7.0            # compatibilité avec dbt-postgres

# === Machine Learning ===
scikit-learn==1.4.2           # clustering k-means + régression
numpy==1.26.4                 # calcul numérique

# === Visualisation / App ===
streamlit==1.32.0             # application web interactive
plotly==5.22.0

# === Configuration ===
python-dotenv==1.0.0          # charger variables d'env depuis .env

# === Qualité du code ===
black==24.3.0                 # formatage automatique du code
flake8==7.0.0                 # linting (détection d'erreurs de style)
isort==5.13.2                 # tri automatique des imports
mypy==1.9.0                   # vérification du typage statique
pre-commit==3.7.0             # hooks git automatiques avant chaque commit

# === Tests ===
pytest==8.1.1                 # framework de tests
pytest-cov==5.0.0             # mesure de la couverture de tests
pytest-postgresql>=5.0.0
